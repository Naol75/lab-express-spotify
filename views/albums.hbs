<div>
{{#each albums}}
<div>
    <img width="200px" src="{{this.images.0.url}}" alt="{{this.name}}">
    <h2>{{this.name}}</h2>
    <a href="/albums/{{this.id}}/tracks">Show tracks</a>
</div>
{{/each}}
</div>